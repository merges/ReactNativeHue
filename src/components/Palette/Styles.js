import React from 'react';
import {
  StyleSheet,
} from 'react-native';

var styles = StyleSheet.create({
  palette: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 5
  }
});

module.exports = styles;
