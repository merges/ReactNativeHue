import React from 'react';
import {
  StyleSheet,
} from 'react-native';

var styles = StyleSheet.create({
  row: {
    alignItems: 'center',
    justifyContent: 'center',
    height: 120
  }
});

module.exports = styles;
